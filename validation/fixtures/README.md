# Validation Fixtures

Stage 4 fixture execution evidence belongs here.

Use only fixtures frozen before the independent builders ran. Preserve failures and corrections rather than rewriting expected results after the fact.
